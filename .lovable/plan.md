

## Change

Update the card background in `FeatureGuide.tsx` (line 42) from `bg-background` to `bg-white` so the feature cards stand out against the sheet's background.

### File: `src/components/FeatureGuide.tsx`
- Line 42: Change `bg-background` → `bg-white` in the card div class.

